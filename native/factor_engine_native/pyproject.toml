[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "factor-engine-native"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "factor_engine_native"
